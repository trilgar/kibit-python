"""
🧺 Завдання: Кошик із фруктами

Уяви собі клас FruitBasket, який моделює кошик із фруктами 🍎🍌🍇. Напиши клас так, щоб:
📌 Можливості класу:

    Можна побачити список фруктів у кошику, просто викликавши print(basket)
    🟢 Використай метод __str__

    Кількість фруктів можна дізнатись через len(basket)
    🟢 Використай метод __len__

    Кошик вважається повним (True), якщо в ньому 3 або більше фруктів
    🟢 Використай метод __bool__

    Зведення до int має повертати кількість фруктів, до float — середню довжину назв фруктів, а до complex — комплексне число, де:

        дійсна частина — кількість фруктів

        уявна частина — середня довжина назв

    🟢 Використай __int__, __float__, __complex__

⚙️ Що реалізувати:

    __init__(self, *fruits) — приймає довільну кількість фруктів як аргументи: FruitBasket("apple", "banana")

    __str__(self) — повертає рядок типу "Кошик із фруктами: apple, banana"

    __len__(self) — повертає кількість фруктів

    __bool__(self) — повертає True, якщо фруктів 3 або більше

    __int__(), __float__(), __complex__() — як описано вище
"""